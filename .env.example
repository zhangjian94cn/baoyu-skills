# WeChat Official Account API credentials
# 微信公众号 API 凭证（从 mp.weixin.qq.com → 设置与开发 → 基本配置 获取）
WECHAT_APP_ID=your-app-id
WECHAT_APP_SECRET=your-app-secret

# Publishing method: api | browser | remote (default: remote)
# 发布策略：api（API直接）| browser（浏览器CDP）| remote（远程服务器，默认）
# PUBLISH_METHOD=remote

# Remote server configuration (optional)
# 远程服务器配置（可选，默认值如下）
# REMOTE_SERVER_HOST=tencent-server
# REMOTE_SERVER_DIR=~/baoyu-skills
# REMOTE_SERVER_BUN_PATH=~/.bun/bin/bun

# Google Gemini API (for AI cover image generation)
# 从 https://aistudio.google.com/apikey 获取
GEMINI_API_KEY=your-gemini-api-key

# Gemini Web Multi-Account (baoyu-danger-gemini-web)
# 先登录账号: npx -y bun skills/baoyu-danger-gemini-web/scripts/main.ts --account <name> --login
# 查看已登录: npx -y bun skills/baoyu-danger-gemini-web/scripts/main.ts --list-accounts
# 设置默认账号:
# GEMINI_WEB_DEFAULT_ACCOUNT=acc1

# AI 封面生成方案 (--generate-cover)
# api = 官方 Gemini API（需要 GEMINI_API_KEY）
# web = Gemini Web 多账号（需要已登录账号）
# COVER_PROVIDER=api
