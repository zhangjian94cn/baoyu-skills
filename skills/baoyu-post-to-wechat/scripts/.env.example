# ============ 发布配置 ============

# 发布策略: api | browser | remote (默认: remote)
# api     = 微信 API 直接发布（需 IP 白名单）
# browser = 浏览器 CDP 自动化（本地，无白名单限制）
# remote  = SSH 远程服务器发布（通过白名单服务器中转）
PUBLISH_METHOD=remote

# ============ 微信 API 凭证 ============
# 从 mp.weixin.qq.com → 设置与开发 → 基本配置 获取
WECHAT_APP_ID=your-app-id
WECHAT_APP_SECRET=your-app-secret

# ============ 远程服务器 (remote 模式) ============
# SSH 主机别名（需在 ~/.ssh/config 配置）
REMOTE_SERVER_HOST=tencent-server
REMOTE_SERVER_DIR=~/baoyu-skills
REMOTE_SERVER_BUN_PATH=~/.bun/bin/bun

# ============ AI 封面生成 (--generate-cover) ============
# 从 https://aistudio.google.com/apikey 获取
GEMINI_API_KEY=your-gemini-api-key

# 封面生成方案: api | web
# api = 官方 Gemini API（需要 GEMINI_API_KEY）
# web = Gemini Web 多账号（需要已登录账号）
COVER_PROVIDER=api
